<%= form_for(@articulo) do |f| %>
  <div class="container">
  	<div class="row">  
      <% if @articulo.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@articulo.errors.count, "error") %> prohibited this articulo from being saved:</h2>

          <ul>
          <% @articulo.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
     <% end %>
    </div>
    <div class="row">
      <div class="field">
        <%= f.label :titulo %><br>
        <%= f.text_field :titulo %>
      </div>
    </div>  
    <div class="row">
      <div class="field">
        <%= f.label :subtitulo %><br>
        <%= f.text_field :subtitulo %>
      </div>
    </div>
    <div class="row">
      <div class="field">
        <%= f.label :cuerpo %><br>
        <%= f.text_area :cuerpo, class: "form-control" ,rows: "10" %>
      </div>
    </div>
    <div class="row">  
      <div class="actions">
        <%= f.submit %>
      </div>
    </div>
  </div>
<% end %>
